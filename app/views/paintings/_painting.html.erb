<div class="painting">
  <%= painting.title %>
  <%= link_to image_tag(painting.image), painting if painting.image %>
  <div class="name"><%= painting.name %></div>
  <div class="review"><%= painting.review %></div>
</div>
